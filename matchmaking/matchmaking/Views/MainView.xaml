<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="matchmaking.Views.MainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:matchmaking.Views"
    xmlns:utils="using:matchmaking.Utils"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <utils:BoolToVisibilityConverter x:Key="BoolToVis"/>
    </Page.Resources>

    <Grid>
        <NavigationView
            x:Name="NavView"
            IsBackButtonVisible="Collapsed"
            IsSettingsVisible="False"
            PaneDisplayMode="Top"
            SelectionChanged="NavView_SelectionChanged">

            <NavigationView.MenuItems>
                <NavigationViewItem Content="Discover" Tag="Discover">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE721;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Tag="Notifications">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xEA8F;" />
                    </NavigationViewItem.Icon>
                    <NavigationViewItem.Content>
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <TextBlock Text="Notifications"/>
                            <Border
                                Background="Red"
                                CornerRadius="10"
                                MinWidth="20"
                                Height="20"
                                Padding="4,0"
                                Visibility="{Binding HasUnread, Converter={StaticResource BoolToVis}}"
                                VerticalAlignment="Center">
                                <TextBlock
                                    Text="{Binding UnreadCount}"
                                    Foreground="White"
                                    FontSize="11"
                                    FontWeight="Bold"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"/>
                            </Border>
                        </StackPanel>
                    </NavigationViewItem.Content>
                </NavigationViewItem>
                <NavigationViewItem Content="Hot Seat" Tag="HotSeat">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE735;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="Edit Profile" Tag="EditProfile">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE77B;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.MenuItems>

            <Frame x:Name="ContentFrame" />
        </NavigationView>

        <!-- Pop-up banner -->
        <Border
            Background="#FF5A5F"
            CornerRadius="12"
            Padding="16,12"
            Margin="20"
            HorizontalAlignment="Right"
            VerticalAlignment="Top"
            Visibility="{Binding IsPopupVisible, Converter={StaticResource BoolToVis}}"
            Canvas.ZIndex="100">
            <StackPanel Spacing="4">
                <TextBlock
                    Text="{Binding PopupTitle}"
                    Foreground="White"
                    FontWeight="Bold"
                    FontSize="14"/>
                <TextBlock
                    Text="{Binding PopupDescription}"
                    Foreground="White"
                    FontSize="12"/>
            </StackPanel>
        </Border>
    </Grid>
</Page>
